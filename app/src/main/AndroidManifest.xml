<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".YTMusicApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.YTMusic"
        android:supportsRtl="true">

        <!--
            FIX BUG #4 : meta-data gms.car.application déclaré UNE SEULE FOIS
            sur <application>. La duplication sur AutoMediaBrowserService
            provoquait des comportements imprévisibles sur certaines versions DHU.
        -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            PlayerService — lecture audio Media3 (app mobile uniquement).
            Répond uniquement aux clients Media3 (MediaController via SessionToken).
            Android Auto est redirigé vers AutoMediaBrowserService via onGetSession().
        -->
        <service
            android:name="com.ytmusic.feature.player.PlayerService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
            </intent-filter>
        </service>

        <!--
            AutoMediaBrowserService — point d'entrée Android Auto.

            OBLIGATOIRE : android.media.browse.MediaBrowserService
            C'est l'action que cherche Android Auto pour détecter les apps audio.
            Sans cet intent-filter, l'app n'apparaît pas dans Auto.

            PAS de foregroundServiceType : ce service ne démarre jamais
            en foreground lui-même. Il délègue la lecture à PlayerService
            via MediaController. Évite InvalidForegroundServiceTypeException
            sur Android 14+.

            PAS de meta-data gms.car.application ici : il est sur <application>.
        -->
        <service
            android:name="com.ytmusic.feature.player.AutoMediaBrowserService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- FileProvider pour les thumbnails -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <meta-data
            android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="false" />

    </application>
</manifest>